# Get your Telegram API credentials from https://my.telegram.org
# 1. Login with your phone number
# 2. Go to API development tools
# 3. Create a new application to get API_ID and API_HASH
API_ID=your_api_id_here
API_HASH=your_api_hash_here

# Your phone number with country code (e.g., +66968166403)
PHONE_NUMBER=+your_phone_number_with_country_code

# Your country's nation code (e.g., 66 for Thailand, 1 for USA, 44 for UK)
NATION_CODE=your_simplus_account_nation_code

# Your Simplus referral/invitation codes to earn referrals
# Get this from your Simplus account's referral section
# OPTIONAL: Used only for initial setup. After first run, manage codes via Settings page in web UI
# You can add multiple codes separated by commas (each gets one referral per cycle)
# Example: CODE1,CODE2,CODE3,CODE4,CODE5
# Leave empty to add codes later via web UI
INVITATION_CODES=

# Keep-alive URL - Your Render app URL to prevent it from sleeping
# Example: https://your-app-name.onrender.com
KEEP_ALIVE_URL=https://your-app-name.onrender.com

# Web UI Authentication - Change these for security!
WEB_USERNAME=admin
WEB_PASSWORD=change_this_password

# Secret key for Flask sessions - Generate a random string for production
# Use: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-random-secret-key-here